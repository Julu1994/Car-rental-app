import React from "react";
import styled from "styled-components";

const Li = styled.li`
    width: 100%;
`;

const Button = () => {
    return (
        <div>
            <Li>Car</Li>
        </div>
    );
};

export default Button;
